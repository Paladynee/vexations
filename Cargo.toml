[package]
name = "vexations"
version = "0.1.0"
edition = "2024"

[features]
default = []
# enable shrink_to_fit after lexing
save_memory = []

[dependencies]
printerator = "1.0.1"
voxell_rng = "0.6.0"
